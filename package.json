{
  "name": "public-docs",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync-openapi": "cp src/openapi/bithuman.yaml public/api/openapi.yaml",
    "predev": "npm run sync-openapi",
    "prebuild": "npm run sync-openapi",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.3.1"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
